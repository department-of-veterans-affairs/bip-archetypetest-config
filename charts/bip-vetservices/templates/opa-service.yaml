{{- if (.Values.opa.enabled) -}}
apiVersion: v1
kind: Service
metadata:
  name: opa-{{ template "bip-archetypetestconfig.fullname" . }}
  labels:
    app: opa-{{ template "bip-archetypetestconfig.name" . }}
    chart: {{ template "bip-archetypetestconfig.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.opaService.type }}
  ports:
    - port: {{ .Values.opaService.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: opa-{{ template "bip-archetypetestconfig.name" . }}
    release: {{ .Release.Name }}
{{- end }}